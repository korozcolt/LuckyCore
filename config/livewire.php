<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Component Layout
    |--------------------------------------------------------------------------
    |
    | This layout is used when rendering a single Livewire component as an
    | entire page via `Route::livewire()`.
    |
    | We use the public layout so account settings match the main site design.
    |
    */
    'component_layout' => 'layouts.public',
];
